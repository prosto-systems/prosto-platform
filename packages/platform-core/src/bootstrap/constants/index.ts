/**
 * @alpha
 * Enum representing the different stages of the bootstrap process.
 */
export enum BootstrapStage {
  Discover = 'discover',
  Validate = 'validate',
  Resolve = 'resolve',
  Lifecycle = 'lifecycle',
}
